# ==============================
# 灵棋经占卜应用 - 环境变量配置示例
# ==============================

# 说明：复制此文件为 .env 并根据实际情况修改配置

# ================
# 前端配置
# ================

# Gemini API 密钥（用于 AI 占卜解读）
# 获取地址: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# 前端服务端口（默认 4000）
FRONTEND_PORT=4000

# ================
# 后端配置
# ================

# 后端服务端口（默认 3001）
SERVER_PORT=3001

# JWT 密钥（用于用户认证，生产环境必须修改）
# 建议使用强随机字符串，例如: openssl rand -base64 32
JWT_SECRET=please-change-this-to-a-strong-random-secret-in-production

# Node 环境（production | development）
NODE_ENV=production

# 数据库连接字符串（SQLite，存储在容器的 /app/data 目录）
DATABASE_URL=file:/app/data/prod.db
